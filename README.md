make sure to run the frontend and backedn simultaneously in two different terminals.
npx ng serve -- frontend
Setting up the JSON server: ● Run the npm install json-server command in your project directory ● Create the db.json file in the same directory to read and write the data ● Run the npx json-server --watch -p 3000 db.json command to start the JSON server ● Provide db.json with dummy data for insurance plans to set up the JSON server
